# Web framework
fastapi>=0.115.0,<2.0.0
uvicorn[standard]>=0.30.0,<1.0.0
python-multipart>=0.0.12

# Agent framework
langgraph>=0.2.28,<1.0.0
langgraph-checkpoint-sqlite>=1.0.3
langgraph-checkpoint-postgres>=2.0.0,<3.0.0
langchain>=0.3.0,<1.0.0
langchain-core>=0.3.0,<1.0.0

# Database
sqlalchemy>=2.0.0,<3.0.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.0
alembic>=1.13.0
pgvector>=0.3.0

# Document parsing
pypdf>=4.3.0
python-docx>=1.1.0
python-magic>=0.4.27

# MCP server — must NOT be pinned to 1.0.0; that version conflicts with
# fastapi's starlette requirement when pip resolves in late 2025+.
# Pinning to <2.0.0 keeps the stdio API stable while allowing starlette resolution.
mcp>=1.0.0,<2.0.0

# HTTP client
httpx>=0.27.0
requests>=2.32.0

# Validation
pydantic>=2.7.0,<3.0.0
pydantic-settings>=2.5.0,<3.0.0

# Utils
python-dotenv>=1.0.0
structlog>=24.4.0

# Embeddings (RAG pipeline — text-embedding-3-small, 1536 dims)
openai>=1.51.0,<2.0.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-mock>=3.14.0
